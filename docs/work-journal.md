# 工作日志：活动、关键画面与来源总结

2026-09-08，工作区实现，尚未进入现有 rc.3 安装包。

## 活动回看迭代（9 月 8 日）

- 活动轨迹默认显示“事项视图”，可切回“原始记录”。事项复用手动生成的带来源总结，串联跨应用的相关记录；未被总结覆盖的活动继续显示为待整理线索，不会仅因应用相同而合并。搜索支持事项标题、正文、分类、备注及应用名；每页 100 项。
- 点击事项、原始活动、总结或问答的来源、关键画面，打开统一的详情面板。面板显示相关总结、原始活动、关联画面及所选画面的 OCR。支持前后翻图、左右方向键、按时间位置拖动和放大画面；不加载整个事项的 OCR 正文，仅按需读取当前画面。
- “修正事项”可保存标题、分类与备注，原始窗口标题和 OCR 保持不变。修正也显示在日志总结和复制日志中。重新生成时，通过唯一的来源多数重合匹配保留修正；无法明确匹配时，修正作为独立事项保留，不强行转移到其他事项。人工备注只在本机展示，不会自动加入后续 AI 分析。
- 支持删除单条活动及其关联画面，或只删除一张画面和 OCR；均需明确确认。删除会取消在途分析、清理引用被删证据的人工修正，并清空可重新生成的总结缓存。其他来源的修正保留。完整活动跨午夜时，单条删除也会删除整个片段。删除后清空窗口内问答缓存，避免继续展示被删除的来源。
- 顶部导航收紧，按日删除收进“更多”；点击应用分布可筛选原始活动。窄窗口展示紧凑概览和可展开应用分布，详情使用全部可用宽度。
- 问答草稿与答案在本次窗口内按日期保留，切换标签或往返日期不会丢失；关闭窗口后不保留，也不写入长期记忆。
- 数据库新增版本 4 的人工修正表。旧记录保持可读，已有记录开关、暂停和采集配置保持不变。

本轮仍由用户手动触发 AI 整理；没有增加后台收费分析。事项的时间范围包含可能的间隔，展示的采样时长只累加其关联活动。不同事项可能引用相同活动，因此不能把各事项时长直接相加作为总工时；当天概览始终按原始活动统计。

本轮验证：完整单测 293 项、Node/renderer 类型检查、生产构建、真实 Electron 冒烟通过。使用隔离配置和合成记录验证事项来源关联、修正持久化及重新生成、问答跨标签/日期保留、单条活动/画面删除与晚到结果处理、画面切换取消旧删除确认。亮暗主题 1024/560 窗口的事项、详情及编辑界面已截图复核，检查无横向溢出；产物在 `temp/journal-v3-ui/`。未更改用户实际记录设置，未调用真实云端模型，尚未重新打包安装包。

## 使用

从聊天顶栏“工作日志”、设置导航“工作日志”、托盘菜单或 `/journal` 打开独立窗口。按用户最新要求，首次默认开启活动、前台窗口画面和本地 OCR；支持手动暂停、只记录标题或彻底关闭。界面及托盘显示当前记录范围和画面异常。

- 完整记录模式约每秒检测前台应用和标题，合并相邻同应用、同标题的采样为活动片段；仅活动模式约每 5 秒检测。
- 按本机日期查看，搜索应用名和窗口标题，超过 100 条分页展示。
- 记录设置支持进程排除名单，以及 7/30/90 天保留期限。缩短保留期限会清理过期记录。
- 可在日志窗口或托盘暂停/继续。手动暂停会持久化，锁屏解除不会自动解除手动暂停。
- 锁屏、系统休眠期间暂停；连续 2 分钟无键鼠活动时不继续记时。采集失败单独显示错误，不伪装为空闲。
- 关闭窗口不停止已开启的记录；退出应用会停止 helper 并关闭数据库。再次启动按已保存的开启/暂停状态恢复。
- 默认排除若干密码管理器进程，以及 ChouYu 自己的前台窗口。排除只对当前应用进程生效，不识别网页类型。
- “删除当天记录”需确认，删除所有与当天重叠的完整片段；跨午夜片段会一起删除。若记录仍开启，当天之后的新活动仍会写入。

## 当前记录范围

默认记录应用名称、窗口标题、采样时间及前台窗口画面，并在本机识别文字。数据保存在本机用户数据目录 `journal/journal.db`，图片位于 `journal/media/`，当前未额外加密。AI 总结和问答仍按需手动调用，不自动上传截图或生成收费分析。

画面目标间隔默认 5 秒，可选 5/10/20/30/60 秒。检测到窗口或标题变化后，稳定约 1 秒补采；采集任务串行，处理较慢时延后，不积压并发采图。1 秒是检测后的稳定时间，不保证覆盖所有短暂切换。只保存 HWND 匹配的前台窗口，采图前后复核进程、窗口和标题；不回退为整个桌面。同一活动中 JPEG 字节完全相同的图片只保留一份，未做感知去重或视频录制。当前未自动遮蔽窗口内部的密码或其他隐私字段。

“关键画面”可按日期和应用、标题、本地 OCR 文字搜索，每页 30 张，点击回看保存的 JPEG 和识别文字。Windows OCR 常驻隐藏进程逐张处理，不调用云服务；需本机可用的 OCR 语言包。暂停时后台 OCR 队列也暂停，单张图片仍可手动识别或重试。

画面空间上限默认 2 GB，可选择 256 MB/512 MB/1 GB/2 GB；达到上限后停止新增画面并显示提示，活动记录继续。保留期限默认仍为 30 天，但空间上限可能先达到，不能保证保存满 30 天。该上限统计图片及待清理图片，不是整个数据库的硬配额。期限清理和按日删除同步清理图片及 OCR 索引，并清空可重新生成的总结缓存；文件占用导致清理不完整时明确报错。启动时清理孤立的临时图片。

数据库升级至版本 3 时，旧版“画面关闭＋20 秒”的默认预设一次性迁移为完整记录＋5 秒，原默认 512 MB 同步改为 2 GB；保留手动暂停、排除名单、保留期限和其他自定义间隔。更早未保存画面字段的配置会补入新默认值。升级后保存的关闭选择不会在下次启动被重新打开。

“日志总结”手动调用当前聊天配置的模型，将所选日期的应用、标题和 OCR 文字发送至已配置的 AI 服务，不发送图片。新版按项目和事项归并，展示具体标题、活动/进展/决定/待解决类型、建议继续入口和可点击来源；可复制日志。旧版总结仍可查看，界面提示重新生成。仅标题证据的事项不会标记为已确认进展。

输入在整个日期范围内抽取约 200 个活动与 100 张画面，并保留范围两端；单张文字最多 3000 字，总计约 40000 字符。相同文字归组但保留各次来源和时间，不再简单丢弃较早记录。覆盖提示展示输入与可用来源数量、含正文来源数量；抽样可能遗漏事项。引用校验不能保证模型每句话都正确。

“问问这一天”支持针对所选日期的一次提问，发送问题与该日抽样文字至当前模型，回答附原始活动或画面来源。回答在本次窗口内按日期保留，不写入长期记忆。当前不是跨日语义检索或多轮聊天；未找到不能说明没有发生。总结和问答串行执行，最长等待 120 秒，可手动取消；普通聊天仍默认 60 秒。删除、锁屏等状态变化会取消在途日志分析。

宽窗口右侧展示当天概览、应用采样时长和证据情况，窄窗口在主内容上方显示紧凑概览和可展开的应用分布。总结缺少 OCR 正文时明确说明只能找回活动线索。布局参考 Dayflow 的内容层次，沿用 ChouYu 主题，没有复制第三方界面代码或增加效率评分。

尚未记录键盘内容、独立网页 URL、文件修改；没有自动定时日报或自动长期记忆写入。

时长是采样估计：短于采样间隔的切换可能遗漏，窗口切换和空闲阈值也会带来误差，不能作为精确工时或产出统计。当前未做全天持续运行资源评测。

## 实现

内置可选 journal 模块；独立常规窗口展示时间线；常驻 PowerShell helper 通过 Win32 查询前台窗口。Electron desktopCapturer 只枚举窗口元信息（缩略图尺寸为 0），再由独立、隐藏的沙盒页面通过 getDisplayMedia 对唯一匹配的 HWND 取一帧，不再为所有候选窗口生成缩略图。采集页面无 IPC bridge、无网络访问、无音频采集；每帧结束后停止视频流，暂停、锁屏、删除或退出时销毁采集页面。单次等待上限 8 秒，尚未结束的元信息枚举不并发重试。相同目标持续失败时按 5/10/20/30 秒退避，切换目标可重新尝试；真实错误仍在界面显示。SQLite、图片写入和清理在独立 Worker 中执行，OCR 在独立常驻 PowerShell 进程执行。脚本以隐藏窗口、最小环境启动，不降低系统执行策略。系统限制运行脚本时显示错误。

9 月 8 日采集优化验证：完整单测 298 项、类型检查、构建与 Electron 冒烟通过。新增窗口元信息枚举、精确来源选择、来源不存在不回退、超时保持串行、取消后丢弃晚到画面、失败退避测试；真实隔离窗口验证连续取帧、取帧后视频流释放、隐藏页面复用及停止后恢复。本轮回归没有出现此前全窗口缩略图枚举触发的 WGC 错误；不保证受保护或已失效的目标窗口可被采集，未做全天 CPU/内存基准测量。

应用启动后按已保存的开启/暂停状态启动 helper；新配置默认开启。空闲时停止活动 helper，恢复活动后重新启动。每个采集周期校验 epoch，防止暂停或删除后的在途结果回写。冒烟测试通过专用初始化参数，在任何采样开始之前关闭测试 profile 的采集，不录制用户活动。

## 验证记录

- 工作日志窗口显式使用 `resources/icon.png`，Windows 应用标识与安装包保持一致。
- 记录设置改为原生模态对话框，支持 Esc、关闭及保存成功后退出；失败时保留设置草稿。事项和原始记录列表提供独立的细滚动条。
- “AI 用量”按所选日志日期展示总结及问答的调用历史，包含请求/返回模型、服务商、时间、状态和输入/输出/总 token。历史存储在 SQLite，重新生成不会覆盖旧调用，删除日期及保留期限清理同步处理。调用中断保留已返回用量，未返回值不记为零，旧版本用量不补算；不保存问题正文或凭据。
- 流式用量字段依据 [OpenAI Docs](https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create) 和 [Claude streaming 文档](https://platform.claude.com/docs/en/build-with-claude/streaming)。Claude 累计计数覆盖更新，缓存输入单独展示并计入总量；仅在兼容接口明确拒绝用量参数时移除该参数重试。
- 本轮 305 项测试、类型检查和构建通过；真实 Electron 验证模型用量持久化、跨连接读取、删除后晚到结果不重建记录、设置 Esc 关闭及两种 100 条长列表独立滚动。亮暗主题 1024/560 截图位于 `temp/journal-v4-ui/`。

- 新增默认关闭、设置校验、手动暂停与锁屏组合、排除应用、空闲、暂停后晚到结果及数据库故障停止采集测试。
- 完整单测 285 项通过，类型检查通过；新增默认配置与单次迁移、稳定后补采、5 秒目标间隔、不并发采图。继续覆盖采图晚到、同窗口切换标题、手动 OCR 删除竞态、总结/问答引用、标题证据降级、预算抽样、模型取消及 Provider 超时。
- Electron 冒烟使用隔离 profile 和合成活动，验证真实 SQLite Worker、合并时段、中文/百分号搜索、跨日时长、分页、删除以及单实例日志窗口。
- 第二阶段 Electron 回归通过：原生采集测试窗口、JPEG 去重、OCR 文字查询、图片 IPC 读取、详情弹窗、总结来源跳转、删除文件/索引/总结，以及晚到 OCR/总结不能恢复已删除内容。`--ocr` 使用固定图片验证同一 Windows OCR 进程连续识别两次。总结界面使用合成缓存；真实模型调用另由下列独立报告验证。
- 原生 helper 返回前台窗口元数据验证通过；该结果不写入数据库或测试日志。未对用户历史开启持续记录。
- 已查看亮色 1024 和暗色 560 窗口截图；产物在 `temp/journal-ui/`。这不替代物理多屏、全天运行和打包安装验收。
- 第二阶段亮暗主题、1024/560 窗口回归无横向溢出；已查看亮色总结和暗色窄窗口关键画面截图，产物在 `temp/journal-phase2-ui/`。

- 使用当前配置的 `deepseek-v4-flash` 完成合成日志实测：输出格式、来源引用和未完成事项表述通过。仅发送合成证据，未上传用户活动历史。报告见 [模型实测](evaluations/journal-live-2026-09-07.json)。

- 本轮真实 Electron 回归新增：通过循环回送的测试 Provider 实际点击生成总结与问答，验证 IPC、来源、数据库保存和概览统计；450 条记录验证抽样保留日期两端。
- 新版亮暗主题、1024/560 窗口均通过无横向溢出检查，已复核总结卡片及当天概览的截图，产物在 `temp/journal-v2-ui/`。
- 本轮按用户要求，以只读方式读取其已有 169 条真实活动（0 张画面），调用 deepseek-v4-flash 评估新版总结及问答。真实报告仅保存在忽略目录 `temp/journal-v2-real.json`，未覆盖已有总结或更改记录设置。旧版 10 条应用流水账归并为一项有具体报告和文档的工作线索；没有正文时仍不能判断完成情况。
- 含报错、后续通过结果、待解决问题和屏幕指令干扰的合成 OCR 实测通过，见 [新版模型实测](evaluations/journal-v2-live-2026-09-07.json)。一次样例通过不代表整体事实准确率。

下一步：先实际积累画面/OCR 后评估找回和总结质量；做全天资源与恢复测试，再扩展浏览器/文件来源、跨日检索。现有基础功能不依赖云端，生成总结或提问时才调用模型。详见 [接入方案](work-journal-integration.md)。
