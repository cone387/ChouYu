# 本次完整迭代验收

目标：落实项目评审提出的可靠性、聊天体验、发布验证和有选择的能力扩展。上一轮功能实现不能替代后续验收；未验证的项目保持未完成。

## 第一轮：数据可靠

- [x] 取消会话/消息数量的静默截断，正文完整保存。
- [x] 图片独立存储、去重、重启恢复和损坏提示。
- [x] 损坏 JSON 保留、上一代备份恢复、读取失败阻止覆盖。
- [x] 保存失败持续提示、重试与未保存退出提醒。
- [x] 存储边界测试与真实 Electron 保存失败/重试流程。

证据：`src/main/database.test.ts`、`src/main/smoke/storage-smoke.ts`；恢复规则与边界见 `storage-recovery.md`。

## 第二轮：聊天体验

- [x] 全部会话全文搜索，命中早期历史并展示片段。
- [x] 当前会话全文搜索，Ctrl+F、匹配消息筛选、关键字高亮和结果分页。
- [x] 流式回复尊重用户上翻阅读位置，提供回到最新消息入口。
- [x] GFM 表格、删除线、任务列表渲染。
- [x] 提高小字提示/操作的字号与次要文字对比度。
- [x] 设置默认突出连接 AI、记忆和权限，高级能力/插件可展开且仍可搜索。
- [x] 修复视口缩小后聊天/设置面板和宠物位置越界。
- [x] 在最终工具链与构建产物上完成全量单测、UI 冒烟及截图复核（当前 255 项测试；Electron 44、rc.3 包内运行）。

证据：`conversation-search.test.ts`、数据库全文搜索用例、`src/main/smoke/chat-smoke.ts`。后者使用 220 条历史和本地流式 Provider 验证真实 UI；截图使用等效视口，不能充当物理多显示器验收。

## 第三轮：质量与发布

- [x] 修复完整依赖审计发现的运行时/开发工具漏洞，验证原生 SQLite 兼容性（完整审计 0 项；Electron 内 SQLite 启动/迁移通过）。
- [x] 配置与本地一致的 CI Node 版本，完成类型、单测、构建、生产与完整依赖审计（本地通过；未声称远程 CI 已执行）。
- [x] 对话、设置、记忆和授权主流程的 UI、键盘、亮暗、窄屏与减少动画验收（等效视口；记忆 CRUD、授权键盘与选择已覆盖）。
- [x] 打包新 Windows 产物，生成校验和并通过 packaged smoke（最新 `1.3.0-rc.3`，包含后续记忆修复及真实离线 OCR，见 `release-candidate.md`）。
- [ ] 干净 Windows 环境安装、启动、卸载、开机自启与更新链路验收。
  - 实机环境核查和具体步骤已记录在 `windows-manual-acceptance.md`。当前未取得可用干净测试系统；准备步骤不算验收通过。
- [ ] 真实 OpenAI-compatible、Claude 和 BBTalk 服务验收。
  - 已配置兼容服务的模型列表、流式完成、取消和纯工具往返通过；其他协议与服务仍待验证，见 `provider-acceptance.md`。
- [ ] 物理多屏、100%/125%/150% DPI、截图定位、快捷键冲突及点击穿透验收。
  - 当前 Electron 实际识别为单屏 125%，见 `evaluations/display-inventory-2026-09-07.json`。其他组合仍未验证。
- [x] 记录代码签名方案和明确的新版本发布状态（本地未签名候选包，未发布；正式签名尚待维护者提供身份）。

不把本地 mock/等效视口/启动成功当成真实服务或另一台机器的验收结果。安装包与发布记录以本次实际生成的产物为准，不能沿用旧版本产物。

## 第四轮：有选择地扩展

- [x] 为长期记忆加入带标注语料的提取/召回/冲突评测，并记录指标与失败案例。
  - 已增加 30 条提取、10 条关键词召回、8 条冲突的基线；真实 `deepseek-v4-flash` 三轮各 15/16，通过率与全部失败已记录，见 `memory-evaluation.md`。
  - 已补充真实 Mem0：12 条记忆、14 个同义/跨语言/主体/时间查询，Recall@3=100%、Precision@3=36.11%，无答案拒绝 0/2；回答依据规范化后 14/14。评测与失败记录已完成，不表示检索质量已达到零误召回；报告和边界见 `memory-evaluation.md`。
- [x] 完成本地离线 OCR 的可用性验证、明确开关/数据边界、失败反馈与相应测试。
  - 手动入口、WinRT 本地识别、超时清理和错误反馈已实现，真实 OCR UI 与 `rc.2` 包内识别通过；支持条件见 `offline-ocr.md`。
- [ ] 在核心稳定后推进外部能力进程隔离、超时与故障恢复，验证真实接入边界。
  - 已完成受信宿主使用的 JSONL 传输层与真实子进程故障/重连测试；第三方 Mem0 OSS 适配与插件分发未开放，边界见 `capability-plugin-guide.md`。
  - 新发现 Mem0 主引擎编辑/删除未正确写入远端、后台写入失败未反馈；需要先修复并做真实隔离用户验收，见 `provider-acceptance.md`。
  - 已修复单条编辑/删除、普通新增及候选确认；真实隔离用户 CRUD 与重启后删除通过，测试数据已清理。批量导入、冲突替换、历史恢复、清空及跨用户缓存仍待处理，主引擎门禁保持未完成。
  - 后续已完成按用户/服务独立缓存、切换与晚到结果隔离、历史恢复确认、无冲突批量导入失败重试。历史恢复真实服务通过，其他新增边界通过 Electron/SQLite 运行时验证；冲突、归档恢复、清空和遗留同步调用仍待补齐。
  - 归档/重新启用和冲突替换已接入远端属性确认，真实服务与部分失败重试验收通过；自动身份替换和导入冲突也等待完成。清空和遗留同步调用仍需处理，不标记整个主引擎已完成。
  - 清空已实现远端删除读回与用户隔离；后台追加写入已移除，旧同步入口在变更前拒绝，真实清空和 Electron 回归通过。远端管理 UI 目前仍仅概览/连接，不能把主服务验收等同于完整 UI 验收。最新工作区单测 255 项。

按照评审建议，多步骤 Agent、工具市场、语音和 3D 角色继续排在核心可靠性之后；不以无验证地扩张功能作为本次质量迭代的完成标准。仓库长期路线中的这些项目没有因此被标记为已完成。

## 持续保留的边界

- 旧版本已经删除的消息/图片无法重建。
- 一代快照备份不是完整版本历史。
- 附件清理需要同时保护当前快照、备份和待恢复文件；极大历史库性能需要独立测量。
- 最终完成声明必须逐项对照以上清单和当前产物，不能仅引用某次绿色测试结果。
