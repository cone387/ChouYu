# 工作日志本轮验收核对

核对日期：2026-09-09。以 `roadmap.md` 顶部已确认的三轮迭代为范围。下表区分已经验证的功能、隔离测试的边界和仍缺的外部验收，不将历史发布记录当成当前产物验收。

| 要求 | 当前证据 | 结论与边界 |
| --- | --- | --- |
| 收藏、接续卡、备注与完成状态独立保存 | `saved.ts`、容量/并发回归、`journal-smoke.ts` | 功能及隔离 Electron 流程通过；模型建议不自动标记完成 |
| 升级、重开后保留收藏图片和个人整理数据 | `journal-migration-smoke.ts`，`temp/journal-migration-smoke.log` | 代表性版本 7、8、9 样本升级至 11，连续两次 worker 打开后图片字节、收藏元数据、项目手动归属和手册版本保持一致 |
| 原日志到期不删除独立收藏 | 同一升级回归，将 30 天保留期改为 7 天 | 8 天前的活动、画面行及原图文件清理，独立收藏图片仍可读取；手册引用不变成缺失 |
| 拒绝不支持的未来数据库 | 同一升级回归，版本 12 样本 | 启动失败并提示升级应用，版本号和既有样本数据保留 |
| 快捷键到真实外部窗口的抓取链路 | `bookmark-shortcut.test.ts`、已完成原生自有窗口短测 | 逻辑和部分原生路径有证据；实际全局按键、物理多屏及 macOS 仍待外部验收 |
| 全天资源使用和恢复 | `journal-soak.cjs` 及隔离长测报告 | 已按用户要求延后 24 小时验收。原进程退出，最后记录约 4 小时 37 分钟、3330 次循环、0 次失败、4 次脚本恢复；报告未写入终态，不能声明全天完成。它不覆盖完整应用或系统休眠 |
| 跨日关键词、OCR、应用筛选、来源定位 | `journal-smoke.ts` 中跨日搜索和昨日来源打开断言 | 隔离真实界面回归通过 |
| 多轮日志问答的范围、引用及删除保护 | `question-context.test.ts`、日志问答回归 | 上下文与来源保护通过；不是实际模型答案质量评测 |
| 语义检索与来源向量复用 | `semantic-search.test.ts`、`journal-semantic-cache-smoke.ts`、本地 HTTP/界面回归 | 明确确认、缓存隔离、来源清理、取消和重复查找只发送查询已验证 |
| 真实语义召回及无答案情形 | 12 个合成标注问题与评测命令 | 仅有人工向量的管线证据。此次检查评测 URL/key/model 环境变量均未配置；真实模型质量及拒答能力未验收 |
| 项目归属修正、手册确认写入记忆、可编辑周报 | 项目/手册/周报数据及 UI 回归 | 本地功能与隔离服务路径通过；Mem0 托管账户端到端验收仍缺 |
| 完整记忆管理与旧记录去重 | `memory-full-data-smoke.ts`、分页回归 | 超过 2000 条的完整导出、去重及冲突重试通过；导入仍保留明确的单次条数/文件大小限制 |

本次新增升级测试使用隔离合成 SQLite 样本与实际日志 worker，不访问用户旧数据库，也不等同于所有历史安装包到新安装包的升级测试。原生窗口采集、系统权限、物理多屏、系统休眠以及真实服务条件仍按原计划保留，不因为单测和合成回归通过而勾选完成。

验证记录：450 项现有单测保持通过；新增升级回归、类型检查、生产构建及完整 Electron 回归通过（`temp/journal-migration-smoke.log`，标记 `CHOUYU_JOURNAL_MIGRATION_SMOKE_PASSED`）。本轮没有发布安装包、推送代码或访问真实云端账户。
