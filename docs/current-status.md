# 当前功能状态

## 2026-09-08 全局导航工作区

工作区源码已改为左侧 56px 纯图标导航（会话、活动、记忆、底部设置）。右侧按模块布局；会话使用列表＋聊天区，活动和记忆使用完整内容区。切换时保持外框和页面状态，托盘日志入口进入同一工作区。布局与交互说明见 [全局导航工作区](workspace-navigation.md)。本次修改尚未生成新安装包。

更新日期：2026-09-07

当前版本：`1.2.0`（已发布）

工作区候选版本：`1.3.0-rc.3`（未发布，本地 Windows 安装包已生成并通过包内 OCR/聊天/记忆回归）。

最新默认配置调整：按用户要求默认开启活动、前台窗口画面及本地 OCR，目标间隔 5 秒；窗口变化后稳定约 1 秒补采，串行调度避免并发堆积。默认画面上限改为 2 GB，期限保持 30 天；界面/托盘显示完整记录、仅标题或画面异常。数据库版本 3 一次性迁移旧默认配置，之后保留用户关闭和暂停选择。285 项单测、类型检查、构建及隔离 Electron 回归通过；已正常重启本机开发构建，确认当前用户配置为开启、5 秒、2 GB。不是全天性能结论。

工作日志基础已提交推送至 `609951f`。本轮继续改为紧凑工作台、当天概览和证据覆盖提示，增强事项总结与继续入口，加入按日单次问答、来源回看及分析取消。282 项单测、类型检查和 Electron 回归通过；deepseek-v4-flash 已实际分析用户现有 169 条活动，以及含正文的合成样例。真实记录没有画面/OCR，暂只能恢复项目与文档线索，不能确认成果。未改动用户记录设置，未进入已有安装包，全天资源和恢复验收仍待进行。见 [使用与边界](work-journal.md)。

最新 UI 调整（尚未进入已有安装包）：输入区上沿支持拖拽、记住高度及双击恢复自动高度；去掉独立横条，输入区与聊天区背景一致，侧栏/输入区分隔线同色。会话列表展开时普通 Esc 直接关闭面板。能力中心与插件入口直接显示在设置导航中；记忆中心默认显示记忆库及状态数量，Mem0 明确标注缓存范围。类型检查、255 项单测、构建和本地 Electron 交互回归通过。下一批建议见 [迭代路线](roadmap.md)。下方旧批次的高级设置收起、Mem0 仅概览/连接布局已被本批替代。

安装包生成后的搜索栏优化：取消聚焦光圈，以底色和放大镜反馈状态；关闭/清空按钮融入搜索框，顶部搜索按钮支持开启状态与切换。已通过类型检查、源码构建的 Electron 回归及亮暗/窄屏截图复核；这项最新 UI 修改尚未进入现有 rc.3 安装包。

`rc.3` 集成 `rc.2` 之后的记忆提取、Mem0 确认写入及隔离、虚拟列表修复。当前单测 255 项通过，完整依赖审计 0 项。真实提取/召回评测与全部失败见 `memory-evaluation.md`，安装包及哈希见 `release-candidate.md`。下方按批记录中的“尚未进入 rc.2”是历史状态，这些代码修正现已进入 rc.3。

## 聊天体验与质量迭代

- 最新工作区：253 项单测通过；Mem0 单条编辑、删除、普通新增和候选确认已加入远端确认，真实隔离用户测试通过且清理成功。批量与生命周期操作仍待修复，详见 `provider-acceptance.md`。这些修改尚未包含在 `rc.2` 安装包中。
- 修复退出全文搜索后虚拟列表可能停在中段的问题：滚动条到达底部时直接定位最后一组消息，列表和占位元素保留高度。此前连续两次 Electron UI 回归失败，修复后同一条完整回归通过。
- Mem0 按服务和用户隔离 SQLite 缓存，支持切换后自动加载和拒绝旧请求晚到结果；历史版本恢复先确认远端，无冲突导入失败后可重试。新边界与完整 UI 回归通过，历史恢复真实服务验收通过；旧库保留，迁移边界见 `provider-acceptance.md`。
- 归档、重新启用、冲突替换/并存改为等待远端确认；归档记录保留远端正文但不用于聊天召回。真实服务归档及替换通过，部分失败可重试；清空和遗留同步路径仍待补齐。当前单测 254 项。
- 后续已完成远端清空确认、失败保留和用户隔离，移除后台追加写入，旧同步入口在修改前拒绝；255 项单测及完整 Electron 回归通过，真实服务清空通过且测试数据清理成功。Mem0 管理 UI 仍采用概览/连接布局，主服务证据不能替代完整远端管理 UI 验收。

- 支持全部会话全文搜索和当前会话 Ctrl+F 搜索，匹配片段高亮并分页。
- 流式回复期间上翻阅读不会被自动拉回底部，可点击提示回到最新消息。
- Markdown 支持 GFM 表格、任务列表和删除线；提高小字可读性，设置高级入口默认收起。
- 修复缩小视口后面板位置越界；真实 Electron 覆盖 220 条历史、全文搜索、滚动行为和设置入口，复核亮暗主题及 375px/1024px 等效视口。
- 工具链统一 Node 24、Electron 44、Vite 7 和 Vitest 5；已适配异步剪贴板 API。
- 2026-09-07：247 项单测、类型检查、构建和 Electron UI 冒烟通过；完整 `npm audit` 为 0 项漏洞。
- Windows 离线 OCR 手动入口已实现，识别结果填入草稿并移除已处理图片；已验证包内识别、取消使用图片后的晚到结果、中文临时路径和无 Provider 请求。
- 记忆新增/编辑/搜索/删除、授权焦点循环/拒绝/允许、减少动画以及亮暗窄屏已加入真实 UI 冒烟；授权期间聊天快捷键不再干扰弹窗。
- 受信宿主可使用外部能力 JSONL 传输层，具备环境过滤、请求限制、超时/崩溃/取消和显式重连；尚未开放任意第三方插件执行或完成 Mem0 OSS 接入。
- 新增本地记忆规则的带标注基线评测，详见 [记忆评测](memory-evaluation.md)。真实模型和语义召回效果不能据此推断。
- 完整未完成事项、发布验证及扩展进度见 [迭代验收](iteration-acceptance.md)。

## 未发布的可靠性迭代

- 聊天不再按 100 个会话 / 每会话 500 条消息静默截断，正文不再在持久化时截短。
- 图片附件独立落盘、内容去重，重启后恢复；缺失或损坏图片保留引用并提示。
- 聊天 JSON 保存上一代有效快照，损坏原文件先隔离保留，再尝试备份恢复；读权限问题或较新格式会阻止写入。
- 保存失败持续提示并可手动重试；正常退出时如仍未保存，默认返回处理。
- 新增数据库存储边界测试与实际 Electron 保存失败/重试 UI 冒烟；保存提示已检查亮暗主题和 375px/1024px 等效视口。
- 本轮不包含新安装包发布、全部 UI 视觉回归、多屏/DPI 和真实服务验收；恢复说明见 [聊天数据保存与恢复](storage-recovery.md)。

## 已可用

- 桌面悬浮宠物、拖拽、边缘吸附、点击穿透和位置记忆
- 全局快捷键唤出、托盘菜单和角色右键菜单
- 托盘图标单击、双击和菜单入口统一打开聊天面板，不再控制桌面宠物显示/隐藏
- OpenAI 兼容接口与 Claude 流式对话，AI 网络请求运行在 Main Process
- Provider 连接诊断、`/v1` 自动修正、认证/网络/响应错误区分
- 主界面与设置页共享的可搜索模型选择器，支持键盘导航和无效模型提示
- Markdown、代码块复制与语法高亮、会话消息全量持久化（发送给模型时仅取最近 30 条作为上下文）
- 多会话工作区：新建、切换、搜索、重命名、删除确认和 Markdown 导出
- 会话列表宽度支持拖拽调整并持久化；不同会话的 AI 回复可并行在后台继续，互不覆盖
- 聊天内容区宽度支持独立拖拽调整并持久化，拖拽边界仅显示调整光标，不显示彩色高亮线
- 聊天、会话列表和设置面板首次打开默认居中；用户拖动后保存位置，并在显示器尺寸变化后自动夹紧
- 旧版单会话数据自动迁移到多会话存储
- 首次 AI 配置引导和上下文截断提示
- 记忆写入模式：默认自动写入，支持逐条确认或关闭写入；冲突候选始终需要用户决策
- 自动写入支持谨慎、平衡和宽松三档置信度；默认平衡，低置信度候选进入待确认
- 记忆证据过滤会忽略疑问句、玩笑、假设、第三方转述和无效姓名；不确定表述会降级审核
- “我是谁 / 我叫什么”等身份问题会跨会话优先召回人物记忆，启动维护会归档旧版误提取的无效姓名
- 记忆中心默认只显示启用记忆，待确认和已归档内容可通过状态筛选单独查看
- 停止生成、失败重试和重新生成
- 用户消息编辑重发、停止后继续生成（已在当前代码实现）
- 模型选择器，以及 `/model 模型名` 快速切换
- idle / thinking / talking / happy / sleeping 角色状态
- SOUL.md 人格内容编辑
- 图片与文本附件、文件拖拽、区域截图
- 滚动截图与图像拼接（已在当前代码实现，跨环境效果仍待验收）
- 窗口/屏幕选择截图，以及基于视觉模型的 OCR、总结和翻译快捷操作
- 本次运行最近 5 张截图复用和图片附件快捷处理
- 剪贴板翻译/总结/询问入口、开机问好和久坐提醒
- OpenAI 与 Claude 通用工具调用循环，不支持工具的模型自动回退普通聊天
- 工具风险分级、逐次授权、60 秒超时拒绝和对话执行时间线
- AI 工具操作权限模式：默认确认、自动审核（安全/读取自动执行，写入仍确认）和完全访问
- 内置当前时间、读取/写入剪贴板、选择文本文件、列出窗口工具
- 设置中可完全关闭 AI 自动工具调用
- 插件自动注册为 AI 工具，外部操作统一强制逐次授权
- 设置页工具目录：来源、风险、授权模式与逐工具启用/禁用
- SQLite 本地长期记忆与可插拔 MemoryProvider 接口
- 明确表达的事实/偏好/人物/项目/工作方式候选提取与逐条确认
- 密码、API Key、Token、JWT 默认阻止写入
- 关键词、重要度、时间和使用频率混合检索并注入 Prompt
- 回答下方可展开查看实际使用的记忆来源
- 设置页记忆中心：搜索、筛选、添加、确认、编辑、删除、导出和忘记全部
- 独立 OpenAI 兼容 Embedding Provider，可留空复用主 AI Provider
- SQLite Float32 向量索引、连接测试和现有记忆批量重建
- 55% 关键词/结构化分数与 45% 向量相似度混合排序
- Embedding 不可用时自动降级为关键词检索
- 同类型事实、偏好、项目和工作方式的矛盾/更新候选检测
- 冲突候选必须由用户选择“替换旧记忆”“两条都保留”或“拒绝新记忆”
- 替换时归档旧记忆并移除旧向量，不会静默覆盖
- 编辑、替换和恢复前保存版本历史，支持卡片内确认恢复
- 可配置 50–2000 条记忆容量预算，超出后按重要度、使用时间和反馈归档低价值项
- 新记忆可配置默认有效期，过期记忆自动归档并移除向量索引
- 记忆中心提供低价值建议、多选批量归档、归档原因和恢复使用
- 回答的记忆来源支持“有帮助 / 不准确”反馈，幂等记录并参与后续检索排序
- 本地主题聚类：按类型和关键词相似度分组，展示主题摘要及原始来源
- 检索结果可压缩为主题摘要，Prompt 保留全部原始记忆 ID，支持来源反馈传播
- 聊天记录显示压缩前后的记忆数量和主题来源
- 人工主题：从已确认记忆中多选同类型条目合并，并可拆分自动/人工主题
- 单条记忆可设置永久、7 天、30 天、90 天、180 天或 1 年有效期
- JSON 导入在写入前预览新增、重复、矛盾和更新，逐条选择并存、替换或跳过
- 本地统计概览展示类型分布、近 8 周新增、归档构成、反馈和压缩量
- “不准确”反馈后可直接打开记忆中心并定位原始来源进行修正
- Mem0 主记忆引擎：连接测试、按 User ID 隔离和主引擎单选使用
- Mem0 API Key 使用 safeStorage 本机加密；不提供后台同步、备份或迁移
- 移除百炼和 `qwen-plus` 的首次启动兜底；Base URL、API Key、模型缺一不可
- 未完成 AI 配置时不能发送普通对话，也不能永久跳过配置提醒
- 独立服务能力注册表：记忆引擎和 Embedding 均由 Main Process 能力插件提供
- 内置 ChouYu SQLite、OpenAI-compatible Embedding、Mem0 Platform 和 Mem0 Self-hosted 能力
- Mem0 Self-hosted 使用源码对应的 `/memories` 与 `X-API-Key`，可连接 `localhost:8888`
- 记忆引擎插件缺失时安全退回 SQLite；Embedding 插件失败时继续退回关键词检索
- 聊天面板全面禁止横向滚动：长文本、代码块、表格、URL、截图菜单和模型菜单均自适应换行/视口宽度
- 代码块保留复制功能但改为换行显示，最近截图缩略图改为多行排列
- 能力目录提供当前选中状态、网络访问和是否发送记忆数据的诊断元数据
- 工具栏恢复单行布局；截图菜单保留向上展开能力，不再被聊天容器裁切
- AI 设置“测试连接”一次展示聊天 Provider 与 Embedding 能力诊断；未配置时不发起无意义请求
- 诊断结果明确区分可用、待配置、未启用和不可用，并显示向量维度而不暴露密钥
- 独立能力中心页面，统一展示记忆引擎和 Embedding 的启用状态、网络访问和数据发送边界
- 能力选择入口与记忆中心保持一致，支持从总览直接跳转配置
- 新增 JSONL 进程能力协议清单校验，为 Mem0 OSS 等重型可选能力预留隔离边界，不增加主程序依赖
- Memory Explorer 记忆库视图：总览统计、类型快捷筛选、搜索、最近更新/重要度/使用次数排序
- 独立记忆工作区：从聊天顶栏、设置导航或 `/memory` 进入，按总览、待处理、记忆库、整理和连接分区
- 待处理视图支持配置显示范围（仅待确认与冲突 / 全部记忆），选择会保存在本机
- 记忆卡片支持展开查看来源与依据、置信度解释和远程数据边界，默认保持紧凑
- Mem0 模式下聊天检索直接走远程主引擎；本地记忆库、Embedding、主题聚类、生命周期和待处理操作仅用于 SQLite 模式
- Mem0 Self-hosted 连接测试同时验证远程列表与 `/memories/search`，并区分超时、连接失败、鉴权失败和服务版本不兼容
- 聊天中的“不准确”反馈可直接打开独立记忆工作区并定位原始记忆
- 记忆卡片集中展示状态、有效期、反馈、来源和版本历史，支持新增、编辑、归档恢复、删除和导出
- 卡片内操作保留二次确认和异步状态，不依赖顶层弹窗
- 设置面板不再固定 440px 高度：桌面最多 760×640，按视口自动收缩
- 设置导航支持搜索、方向键、Home/End 和 Esc 清空；人格编辑器扩大并使用细滚动条
- 聊天面板高度和会话列表展开状态会持久化，并在显示器尺寸变化后自动夹紧到可用范围
- 输入框只在进入聊天、新建/切换会话或关闭阻塞流程时获取焦点，不再用延迟重试抢夺其他控件焦点
- 设置内容区独立纵向滚动并禁止横向溢出；窄屏导航与内边距自动收敛
- 长内容、Markdown 代码/表格、超长 URL 的聊天布局回归保护持续生效
- 插件框架与 BBTalk 内置插件
- 开机自启、检查更新和下载进度反馈
- 更新错误会按权限、版本不存在和网络故障分类提示，不直接暴露 Token/鉴权原始错误
- API Key 与插件密钥的本机安全存储（系统支持 `safeStorage` 时）
- 仅宠物模式置顶；聊天和设置面板按普通窗口层级显示
- 隔离临时用户数据的开发产物与打包产物启动冒烟测试

## 需要真实环境验收

- 不同 OpenAI 兼容服务和 Claude 官方接口的模型列表、流式响应兼容性
- BBTalk 登录、Token 刷新和发布
- Windows 安装、卸载、开机自启和自动更新完整链路
- 多显示器、缩放比例和不同分辨率下的截图与面板定位
- 快捷键与其他软件冲突时的恢复行为

## 尚未实现

- 本地离线 OCR、录屏和自动活动窗口跟随
- 外部能力插件安装/签名/依赖隔离、Mem0 OSS 具体运行时和 Letta 适配器
- 多步骤 Agent 规划、后台任务和在线工具市场
- 语音输入、TTS、3D/VRM 角色
- 插件市场、多角色和多设备同步

## 工程验证

当前自动化质量门禁包括 TypeScript 类型检查、228 项 Vitest 单元测试、Electron 生产构建和启动冒烟测试。单元测试覆盖配置归一化和必配判断、能力注册/解析/重复保护、进程能力清单校验、Provider 与 Embedding 分开诊断、Prompt 构建、AI 流解析与 URL 校验、Provider 模型诊断、OpenAI/Claude 工具调用增量、多轮工具续答、多会话请求路由、工具协议、工具权限模式、插件工具命名、记忆候选/敏感过滤/自动写入置信度/证据过滤/身份召回/无效姓名清理/关键词/混合排序/矛盾识别/反馈评分/生命周期整理/主题聚类/人工主题/拆分排除/摘要压缩/Prompt 来源、Embedding 解析与余弦相似度、Mem0 Platform 与 Self-hosted 响应/鉴权/远程搜索与写入、模型搜索、会话标题/搜索/Markdown 导出、捕获源校验与视觉动作提示、附件限制、回复重试与角色状态机、输入焦点策略、聊天布局无横向滚动/工具栏单行、设置面板自适应、设置分类搜索、侧栏/聊天内容区尺寸持久化、统一面板定位和键盘导航保护、LLM 记忆结构化提取、记忆写入状态反馈与身份档案编辑、托盘宠物显示状态和 SOUL.md 版本 Diff、主记忆引擎单选与 Mem0 主引擎连接、Mem0 原始消息托管提取。记忆库视图继续复用已有记忆服务测试。Electron 启动冒烟新增 Mem0 主引擎阶段：主进程内嵌 fake self-hosted server，覆盖引擎切换、连接测试、聊天检索、后台写入，以及 401 认证失败、搜索端点 404 退回列表召回、连接掐断与恢复的完整故障矩阵。

启动冒烟测试会验证能力目录和 SQLite 引擎解析，再在临时用户目录中覆盖多会话迁移及完整记忆工作流。Mem0 网络行为由注入式 Fetch 单元测试覆盖，不会在 CI 或 smoke 中访问真实账户；进程能力协议只做清单校验，不启动外部 helper。生产依赖 `npm audit --omit=dev` 当前为 0 个漏洞。`1.2.0` 已正式发布，包含界面结构收敛（design token、ChatPanel/记忆设置组件拆分）、手动亮/暗主题、设置项级搜索、输入历史、流式渲染节流和代码块语法高亮。
